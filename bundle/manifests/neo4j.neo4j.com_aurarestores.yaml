apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: aurarestores.neo4j.neo4j.com
spec:
  group: neo4j.neo4j.com
  names:
    kind: AuraRestore
    listKind: AuraRestoreList
    plural: aurarestores
    shortNames:
    - aurarestore
    singular: aurarestore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.instanceRef
      name: Instance
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.startTime
      name: Started
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: AuraRestore restores an AuraInstance in place from one of its
          snapshots.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AuraRestoreSpec restores an Aura instance in place from one of its snapshots
              (the Aura equivalent of Neo4jRestore). It is a one-shot action recorded as an
              auditable object: on completion the CR stays as history and does not re-fire.
              API credentials are resolved from the referenced AuraInstance's provider
              config. All fields are immutable.
            properties:
              instanceRef:
                description: InstanceRef is the target AuraInstance (same namespace),
                  restored in place.
                type: string
              snapshotId:
                description: SnapshotID to restore from. Mutually exclusive with snapshotRef.
                type: string
              snapshotRef:
                description: |-
                  SnapshotRef resolves the snapshot ID from an AuraSnapshot CR in the same
                  namespace. Mutually exclusive with snapshotId.
                type: string
            required:
            - instanceRef
            type: object
            x-kubernetes-validations:
            - message: set exactly one of snapshotId or snapshotRef
              rule: has(self.snapshotId) != has(self.snapshotRef)
            - message: instanceRef is immutable
              rule: self.instanceRef == oldSelf.instanceRef
            - message: snapshotId is immutable
              rule: '!has(oldSelf.snapshotId) || (has(self.snapshotId) && self.snapshotId
                == oldSelf.snapshotId)'
            - message: snapshotRef is immutable
              rule: '!has(oldSelf.snapshotRef) || (has(self.snapshotRef) && self.snapshotRef
                == oldSelf.snapshotRef)'
          status:
            description: AuraRestoreStatus is the observed state of the restore action.
            properties:
              completionTime:
                description: CompletionTime is when the instance returned to Running
                  (or failed).
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                description: Message carries the latest human-readable detail.
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: 'Phase: Pending, Restoring, Completed, Failed.'
                type: string
              snapshotId:
                description: SnapshotID actually restored (resolved from snapshotRef
                  when used).
                type: string
              startTime:
                description: StartTime is when the restore was issued to Aura.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
