---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: neo4jdisasterrecoveries.neo4j.neo4j.com
spec:
  group: neo4j.neo4j.com
  names:
    kind: Neo4jDisasterRecovery
    listKind: Neo4jDisasterRecoveryList
    plural: neo4jdisasterrecoveries
    singular: neo4jdisasterrecovery
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.primaryClusterRef
      name: Primary
      type: string
    - jsonPath: .spec.secondaryClusterRef
      name: Secondary
      type: string
    - jsonPath: .status.activeRegion
      name: Active
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Neo4jDisasterRecovery is the Schema for the neo4jdisasterrecoveries
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Neo4jDisasterRecoverySpec defines the desired state of Neo4jDisasterRecovery
            properties:
              crossRegion:
                description: Cross-region configuration
                properties:
                  network:
                    description: Network configuration for cross-region
                    properties:
                      customEndpoints:
                        description: Custom network endpoints
                        items:
                          description: NetworkEndpoint defines a network endpoint
                          properties:
                            name:
                              description: Endpoint name
                              type: string
                            region:
                              description: Region
                              type: string
                            url:
                              description: Endpoint URL
                              type: string
                          required:
                          - name
                          - region
                          - url
                          type: object
                        type: array
                      transitGateway:
                        description: Transit gateway configuration
                        properties:
                          gatewayId:
                            description: Transit Gateway ID
                            type: string
                          routeTableId:
                            description: Route table ID
                            type: string
                        type: object
                      vpcPeering:
                        description: VPC peering configuration
                        properties:
                          autoCreate:
                            description: Auto-create peering connection
                            type: boolean
                          primaryVpcId:
                            description: VPC ID in primary region
                            type: string
                          secondaryVpcId:
                            description: VPC ID in secondary region
                            type: string
                        type: object
                    type: object
                  primaryRegion:
                    description: Primary region
                    type: string
                  replicationMode:
                    default: async
                    description: Replication mode
                    enum:
                    - async
                    - sync
                    type: string
                  secondaryRegion:
                    description: Secondary region
                    type: string
                required:
                - primaryRegion
                - secondaryRegion
                type: object
              failover:
                description: Failover configuration
                properties:
                  automatic:
                    default: true
                    description: Enable automatic failover
                    type: boolean
                  notifications:
                    description: Notification configuration
                    properties:
                      email:
                        description: Email configuration
                        properties:
                          from:
                            description: Sender email
                            type: string
                          recipients:
                            description: Recipients
                            items:
                              type: string
                            type: array
                          smtpServer:
                            description: SMTP server
                            type: string
                        type: object
                      pagerDuty:
                        description: PagerDuty integration
                        properties:
                          integrationKey:
                            description: Integration key
                            type: string
                          serviceId:
                            description: Service ID
                            type: string
                        type: object
                      slackWebhook:
                        description: Slack webhook URL
                        type: string
                    type: object
                  rpo:
                    default: 1h
                    description: Recovery Point Objective (max data loss)
                    type: string
                  rto:
                    default: 15m
                    description: Recovery Time Objective (max downtime)
                    type: string
                  triggers:
                    description: Failover triggers
                    properties:
                      healthCheckFailures:
                        description: Custom health check failures
                        format: int32
                        type: integer
                      primaryUnavailableFor:
                        description: Primary cluster unavailable for duration
                        type: string
                      replicationLagThreshold:
                        description: Replication lag threshold
                        type: string
                    type: object
                type: object
              healthCheck:
                description: Health check configuration
                properties:
                  customChecks:
                    description: Custom health check endpoints
                    items:
                      description: CustomHealthCheck defines a custom health check
                      properties:
                        cypherQuery:
                          description: Cypher query to execute
                          type: string
                        expectedResult:
                          description: Expected result
                          type: string
                        httpEndpoint:
                          description: HTTP endpoint to check
                          type: string
                        name:
                          description: Check name
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  failureThreshold:
                    default: 3
                    description: Failure threshold before triggering failover
                    format: int32
                    type: integer
                  interval:
                    default: 30s
                    description: Health check interval
                    type: string
                  timeout:
                    default: 10s
                    description: Health check timeout
                    type: string
                type: object
              primaryClusterRef:
                description: Primary cluster reference
                type: string
              replication:
                description: Replication configuration
                properties:
                  bandwidthLimit:
                    description: Bandwidth throttling
                    type: string
                  compression:
                    description: Compression settings
                    properties:
                      algorithm:
                        default: lz4
                        description: Compression algorithm
                        enum:
                        - gzip
                        - lz4
                        - zstd
                        type: string
                      enabled:
                        default: true
                        description: Enable compression
                        type: boolean
                      level:
                        description: Compression level (1-9)
                        format: int32
                        type: integer
                    type: object
                  encryption:
                    description: Encryption settings
                    properties:
                      algorithm:
                        default: AES256
                        description: Encryption algorithm
                        enum:
                        - AES256
                        - ChaCha20
                        type: string
                      enabled:
                        default: true
                        description: Enable encryption
                        type: boolean
                      keySecret:
                        description: Encryption key secret reference
                        type: string
                    type: object
                  interval:
                    description: Replication interval for batch mode
                    type: string
                  method:
                    default: streaming
                    description: Replication method
                    enum:
                    - streaming
                    - batch
                    - hybrid
                    type: string
                type: object
              secondaryClusterRef:
                description: Secondary cluster reference
                type: string
            required:
            - primaryClusterRef
            - secondaryClusterRef
            type: object
          status:
            description: Neo4jDisasterRecoveryStatus defines the observed state of
              Neo4jDisasterRecovery
            properties:
              activeRegion:
                description: Current active region
                type: string
              conditions:
                description: Conditions represent the current state of the disaster
                  recovery
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              healthCheckResults:
                description: Health check results
                items:
                  description: HealthCheckResult defines a health check result
                  properties:
                    error:
                      description: Error message if failed
                      type: string
                    lastCheckTime:
                      description: Last check time
                      format: date-time
                      type: string
                    name:
                      description: Check name
                      type: string
                    responseTime:
                      description: Response time
                      type: string
                    status:
                      description: Status
                      type: string
                  required:
                  - lastCheckTime
                  - name
                  - status
                  type: object
                type: array
              lastFailoverTime:
                description: Last failover time
                format: date-time
                type: string
              message:
                description: Message provides additional information
                type: string
              observedGeneration:
                description: Observed generation
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase
                type: string
              replicationStatus:
                description: Replication status
                properties:
                  bytesReplicated:
                    description: Bytes replicated
                    format: int64
                    type: integer
                  errors:
                    description: Replication errors
                    items:
                      type: string
                    type: array
                  lag:
                    description: Replication lag
                    type: string
                  lastReplicationTime:
                    description: Last replication time
                    format: date-time
                    type: string
                  replicationRate:
                    description: Replication rate
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
