---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: neo4jusers.neo4j.neo4j.com
spec:
  group: neo4j.neo4j.com
  names:
    kind: Neo4jUser
    listKind: Neo4jUserList
    plural: neo4jusers
    singular: neo4juser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.username
      name: Username
      type: string
    - jsonPath: .spec.clusterRef
      name: Cluster
      type: string
    - jsonPath: .spec.roles
      name: Roles
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Neo4jUser is the Schema for the neo4jusers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Neo4jUserSpec defines the desired state of Neo4jUser
            properties:
              clusterRef:
                description: Reference to the Neo4j cluster
                type: string
              mustChangePassword:
                default: false
                description: Whether user must change password on first login
                type: boolean
              passwordSecret:
                description: Secret containing the user password
                properties:
                  key:
                    default: password
                    description: Key within the secret containing the password
                    type: string
                  name:
                    description: Name of the secret
                    type: string
                required:
                - name
                type: object
              properties:
                additionalProperties:
                  type: string
                description: Additional user properties
                type: object
              roles:
                description: Roles assigned to the user
                items:
                  type: string
                type: array
              suspended:
                default: false
                description: Whether the user account is suspended
                type: boolean
              username:
                description: Username for the Neo4j user
                pattern: ^[a-z][a-z0-9_]{2,30}$
                type: string
            required:
            - clusterRef
            - passwordSecret
            - username
            type: object
          status:
            description: Neo4jUserStatus defines the observed state of Neo4jUser
            properties:
              appliedRoles:
                description: Applied roles
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the current state of the user
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: CreationTime shows when the user was created
                format: date-time
                type: string
              lastLogin:
                description: LastLogin shows when the user last logged in
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed Neo4jUser
                format: int64
                type: integer
              passwordChangeRequired:
                description: PasswordChangeRequired indicates if password change is
                  required
                type: boolean
              phase:
                description: Phase represents the current phase of the user
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
