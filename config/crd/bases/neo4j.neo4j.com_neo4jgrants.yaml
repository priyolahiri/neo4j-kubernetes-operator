---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: neo4jgrants.neo4j.neo4j.com
spec:
  group: neo4j.neo4j.com
  names:
    kind: Neo4jGrant
    listKind: Neo4jGrantList
    plural: neo4jgrants
    singular: neo4jgrant
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.target.kind
      name: Target
      type: string
    - jsonPath: .spec.statements.length
      name: Statements
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Neo4jGrant is the Schema for the neo4jgrants API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Neo4jGrantSpec defines the desired state of Neo4jGrant
            properties:
              clusterRef:
                description: Reference to the Neo4j cluster
                type: string
              description:
                description: Description of the grant
                type: string
              privilegeRules:
                description: Privilege rules (alternative format)
                items:
                  description: PrivilegeRule defines a privilege rule for granting
                    or revoking
                  properties:
                    action:
                      description: Privilege action (GRANT or REVOKE)
                      type: string
                    graph:
                      description: The graph/database the privilege applies to
                      type: string
                    privilege:
                      description: Specific privilege type
                      type: string
                    qualifier:
                      description: Qualifier for the privilege (e.g., label, property)
                      type: string
                    resource:
                      description: Database or resource the privilege applies to
                      type: string
                    roleName:
                      description: Role name to grant/revoke privilege to/from
                      type: string
                    userName:
                      description: User name to grant/revoke privilege to/from
                      type: string
                  type: object
                type: array
              statements:
                description: Privilege statements to execute
                items:
                  type: string
                type: array
              target:
                description: Target user or role for the grant
                properties:
                  kind:
                    description: Type of target (User or Role)
                    enum:
                    - User
                    - Role
                    type: string
                  name:
                    description: Name of the target user or role
                    type: string
                required:
                - kind
                - name
                type: object
              whenNotMatched:
                default: error
                description: What to do when a privilege doesn't match existing state
                enum:
                - error
                - ignore
                - replace
                type: string
            required:
            - clusterRef
            - statements
            - target
            type: object
          status:
            description: Neo4jGrantStatus defines the observed state of Neo4jGrant
            properties:
              appliedStatements:
                description: Applied statements
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the current state of the grant
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedStatements:
                description: Failed statements with error messages
                items:
                  description: FailedStatement represents a failed privilege statement
                  properties:
                    error:
                      description: Error message
                      type: string
                    statement:
                      description: The statement that failed
                      type: string
                    timestamp:
                      description: Timestamp of the failure
                      format: date-time
                      type: string
                  required:
                  - error
                  - statement
                  - timestamp
                  type: object
                type: array
              lastExecutionTime:
                description: Last execution time
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation observed by
                  the controller
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the grant
                type: string
              statementsHash:
                description: Hash of the current statements for change detection
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
